# Global Workflow（Local Only）

本專案的工作流**只保留在本機**（不包含上傳/發佈流程），並已合併至 Antigravity 的 `new-project` 工作流中。

## /new-project（Chrome / Edge Extension）

核心規則：

- 擴充元件可直接載入的根目錄為 `extension/`（包含 `manifest.json`）
- 開發工具與文件留在 repo 根目錄（例如 `scripts/`、`docs/`、`package.json`）

本專案的資料夾結構規則請見 `docs/project-structure.md`。

### Antigravity 預設位置（本機）

- `C:\\Users\\<USERNAME>\\.gemini\\antigravity\\.agent\\workflows\\new-project.md`
- `C:\\Users\\<USERNAME>\\.gemini\\antigravity\\global_workflows\\new-project.md`
